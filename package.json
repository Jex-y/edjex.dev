{
  "name": "peronsal_website",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "preinstall": "cd hugo/themes/terminal && npm install && npm run build",
    "start:solid": "vite",
    "build:solid": "vite build",
    "serve:solid": "vite preview",
    "start:hugo": "cd hugo && hugo server --baseURL=http://localhost",
    "build:hugo": "cd hugo && rm -rf ./public && hugo"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.7.4",
    "vite": "^3.0.0",
    "vite-plugin-solid": "^2.3.0"
  },
  "dependencies": {
    "solid-js": "^1.4.7"
  }
}
