{
  "name": "peronsal_website",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start:solid": "vite",
    "build:solid": "vite build",
    "serve:solid": "vite preview",
    "install:hugo": "sudo apt install hugo && cd hugo/themes/terminal && npm install",
    "start:hugo": "cd hugo && hugo server --baseURL=http://localhost",
    "build:hugo": "cd hugo/themes/terminal && npm run build && cd ../../ && rm -rf ./public && hugo"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.7.4",
    "vite": "^3.0.0",
    "vite-plugin-solid": "^2.3.0"
  },
  "dependencies": {
    "solid-js": "^1.4.7"
  }
}
